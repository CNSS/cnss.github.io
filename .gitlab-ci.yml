stages:
- deploy

deploy:
  stage: deploy
  image:
    name: node:8-alpine
    entrypoint: ["ash", "-c"]
  script:
  - npm install -g firebase-tools
  - firebase deploy
  environment:
    name: production
  only:
  - tags

